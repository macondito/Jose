const base = 10;
const altura = 5;
const areaRectangulo = base * altura
// la variable de tipo constante arearectangulo, le fue asignado el resultado de la multiplicacion de base * altura, y su resultado es 50 que es un dato del tipo number 

const num1 =8;
const num2 =6;
const num3 =10;
const promedio = (num1 + num2 + num3) / 3;
//la variable de tipo constante promedio, le fue asignado el resultado de la suma de (num1 + num2 + num3) / 3 y su resultado es 8 que es un dato del tipo number

const celsius = 25;
const fahrenheit = (celsius * 9 / 5) + 32
// la variable de tipo cosntante fahrenheit, le fue asignado el resultado de (celsius * 9 / 5) + 32 y su resultado es 77 que es un dato del tipo number

const radio = 7;
const perimetroCirculo = 2 * Math.PI *radio;
// la variable de tipo cosntante perimetroCirculo, le fue asignado el resultado de 2 * Math.PI *radio y su resultado es 43.9823 que es un dato del tipo number


const dividendo = 29;
const divisor = 5;
const residuo = dividendo % divisor;
// la variable de tipo cosntante residuo, le fue asignado el resultado de dividendo % divisor y su resultado es 4 que es un dato del tipo number

let numero = 15;
numero = + 1;
// la variable de tipo let numero, le fue asignado el resultado de numero = + 1 y su resultado es 16 que es un dato del tipo number

const numeroCuadrado = 4;
const cuadrado = numeroCuadrado * numeroCuadrado;
// la variable de tipo constante cuadrado, le fue asignado el resultado de numeroCuadrado * numeroCuadrado y su resultado es 16 que es un dato del tipo number


const velocidad = 60; // km/h
const tiempo = 2; // horas
const distancia = velocidad * tiempo;
// la variable de tipo constante distancia, le fue asignado el resultado de velocidad * tiempo y su resultado es 120 que es un dato del tipo number

